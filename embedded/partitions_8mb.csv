# f-tank partition table (8 MB flash) — "Full" SKU
# Dual OTA app slots enable signed OTA with rollback (FR-38, FR-39).
# 8 MB = 0x800000. ~3 MB per app slot leaves room for TLS/cloud growth; the
# remaining ~1.9 MB filesystem backs local logging (Premium tier).
# Name,     Type, SubType,  Offset,    Size,     Flags
nvs,        data, nvs,      0x9000,    0x5000,
otadata,    data, ota,      0xe000,    0x2000,
app0,       app,  ota_0,    0x10000,   0x300000,
app1,       app,  ota_1,    0x310000,  0x300000,
littlefs,   data, spiffs,   0x610000,  0x1E0000,
coredump,   data, coredump, 0x7F0000,  0x10000,
